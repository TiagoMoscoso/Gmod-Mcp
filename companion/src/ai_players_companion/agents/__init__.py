"""Agent data the Companion holds about each AI Player.

This is a mock, in-memory registry: `gmod-companion-bridge` replaces the
backing store with live GMod data without renaming the MCP tools that read
it (design.md Migration Plan).
"""
